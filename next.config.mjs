/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aft-vbi-pds.s3.amazonaws.com', 'demofree.sirv.com'], // Add other domains as needed
      },};

export default nextConfig;
